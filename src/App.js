import React from '../react';

function App(props) {
    return (
        <div>
            <h1>hi {props.name}</h1>
            <p>This my first react App</p>
        </div>
    );
}

export default App;